# `src/pages/getInfo.css`

**Location**: `src/pages/getInfo.css` (23 lines)

## File Purpose

Dead CSS file. Originally intended to style the phone input on `GetInfo.jsx`, but `GetInfo.jsx` instead injects its own `<style>` block with the same selectors. Not imported by any file.

## Imports

None.

## Internal Logic

Custom styles for the `.phone-input-custom .PhoneInputInput` selectors.

## Used By

Nothing.

## Risks

None. Safe to delete.
