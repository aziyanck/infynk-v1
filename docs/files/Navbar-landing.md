# `src/landing-components/Navbar.jsx`

**Location**: `src/landing-components/Navbar.jsx` (0 lines)

## File Purpose

Empty placeholder file. The landing page builds its own header inline (`<header>…</header>` inside `LandingPage.jsx`).

## Imports

None.

## Exports

None.

## Risks

* The folder exists with a single empty file — can be deleted without side effects. Vite does not load empty `.jsx` files, so this is safe to leave.
